# Copy this file's values into Render's Environment settings — do not
# commit a real .env file with real secrets to source control.

# The ESP32 sends this token to prove check-ins are really from your device.
DEVICE_TOKEN=change-me-device-secret

# The web dashboard sends this token to authorize start/stop/reset/threshold commands.
DASHBOARD_TOKEN=change-me-dashboard-secret
